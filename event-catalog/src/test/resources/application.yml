spring:
  profiles:
    active: test
  cloud:
    config:
      enabled: false
  messages:
    basename: messages
    encoding: UTF-8
application:
  storage: memory
  config:
    assets: assets/images
eureka:
  client:
    enabled: false
