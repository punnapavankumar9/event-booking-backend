spring:
  cloud:
    config:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
application:
  security:
    jwt-secret-key: test-secret-from-test-resource-folder
eureka:
  client:
    enabled: false