spring:
  cloud:
    config:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
application:
  security:
    jwt-secret-key: test-secret-from-test-resource-folder
eureka:
  client:
    enabled: false